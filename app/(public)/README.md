# app/(public)/

Rutas accesibles sin sesión.

Páginas planeadas:
- `/` — landing (en `app/page.tsx`)
- `/login`, `/signup` — auth con Supabase email/password
- `/modulos` — listado público de módulos publicados
- `/modulos/[slug]` — detalle de módulo (con botón Comprar si no inscrito)
- `/verificar/[code]` — verificación pública de certificado
